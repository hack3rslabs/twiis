---
deployment:
  tasks:
    # ⚠️ IMPORTANT: Change "your_cpanel_username" to your actual cPanel username
    - export DEPLOYPATH=/home/your_cpanel_username/public_html/
    
    # This copies everything inside the root folder directly to public_html
    - /bin/cp -R * $DEPLOYPATH
